server:
  port: 9091

spring:
  application:
    name: DEPARTMENT-SERVICE

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
